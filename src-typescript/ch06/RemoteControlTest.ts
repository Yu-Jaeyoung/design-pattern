import {SimpleRemoteControl} from "./SimpleRemoteControl";
import {Light} from "./Light";
import {LightOnCommand} from "./LightOnCommand";
import {GarageDoor} from "./GarageDoor";
import {GarageDoorUpCommand} from "./GarageDoorUpCommand";

class RemoteControlTest {
    static main(): void {
        // const simpleRemoteControl: SimpleRemoteControl = new SimpleRemoteControl();
        // const light: Light = new Light();
        // const lightOnCommand: LightOnCommand = new LightOnCommand(light);
        // const garageDoor: GarageDoor = new GarageDoor();
        // const garageDoorOpenCommand: GarageDoorUpCommand = new GarageDoorUpCommand(garageDoor);
        //
        // simpleRemoteControl.setCommand(lightOnCommand);
        // simpleRemoteControl.buttonWasPressed();
        // simpleRemoteControl.setCommand(garageDoorOpenCommand);
        // simpleRemoteControl.buttonWasPressed();
    }
}

RemoteControlTest.main();